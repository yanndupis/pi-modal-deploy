[project]
name = "pi-modal-deploy"
version = "0.1.0"
description = "Pi skill for deploying Modal-hosted SGLang model providers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.4.2",
]
